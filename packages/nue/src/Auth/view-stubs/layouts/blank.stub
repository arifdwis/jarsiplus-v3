<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>@yield('title') :: {{ config('nue.name') }}</title>
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <link rel="shortcut icon" href="{{ config('nue.brand.favicon') }}">
    <link rel="stylesheet" href="{{ asset('vendor/fonts/fonts.css') }}">

    <link rel="stylesheet" href="{{ asset('vendor/nue/css/vendor.min.css') }}">
    <link rel="stylesheet" href="{{ asset('vendor/nue/css/nue.min.css') }}">
    <link rel="preload" href="{{ asset('vendor/nue/css/nue.min.css') }}" data-nue-appearance="default" as="style">
    <link rel="preload" href="{{ asset('vendor/nue/css/nue-dark.min.css') }}" data-nue-appearance="dark" as="style">

    <style data-nue-appearance-onload-styles>
        *{transition:unset!important}
        body{opacity:0}
    </style>

    <script>
        window.nue_config = {
            "themeAppearance":{
                "layoutSkin": "default"
            }
        }
    </script>

    @yield('css')
</head>

<body class="d-flex align-items-center min-h-100">
    <script src="{{ asset('vendor/nue/js/nue.theme-appearance.js') }}"></script>

    @yield('content')

    <script src="{{ asset('vendor/nue/js/vendor.min.js') }}"></script>
    <script src="{{ asset('vendor/nue/js/nue.min.js') }}"></script>
    <script src="{{ asset('vendor/iconify/iconify.min.js') }}"></script>

    @if(notify()->ready())
        <div class="position-fixed top-0 end-0 p-3" style="z-index: 100">
            <div id="toast" class="toast align-items-center rounded-1 text-white bg-{{ notify()->type() }} border-0" role="alert" aria-live="assertive" aria-atomic="true">
                <div class="d-flex">
                    <div class="toast-body">{!! notify()->message() !!}</div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
            </div>
        </div>
        <script>
            const toast = new bootstrap.Toast(document.querySelector('#toast'))
            toast.show();
        </script>
    @endif

    @yield('js')
</body>
</html>